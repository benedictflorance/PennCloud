syntax = "proto3";

package PennCloud;

message Request{
    string type = 1;
    string rowkey = 2;
    string columnkey = 3;
    optional string value1 = 4; // Used only for PUT and CPUT requests
    optional string value2 = 5; // Used only for CPUT requests
}