syntax = "proto2";

package PennCloud;

message Response{
    required string status = 1; // +OK or -ERR
    optional string description = 2; // Message present if there's an error
    optional bytes value = 3; // Used only for CPUT requests
}